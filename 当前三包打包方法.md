# 医保系统当前三包打包方法

## 一、当前打包产物

系统采用“三包部署”方式。以 `260723-22` 为例，最终产物位于：

```text
/home/ubuntu/yibao/260723yb/部署包/260723-22/
├── yibao_backend_260723-22.tar
├── 260723yb-backend-deploy-22.tar.gz
├── 260723yb-frontend-static-22.tar.gz
└── SHA256SUMS
```

三个部署包分别是：

| 包 | 内容 | 用途 |
|---|---|---|
| `yibao_backend_260723-22.tar` | 后端 OCI/Docker 镜像 | 在目标服务器执行 `docker load` |
| `260723yb-backend-deploy-22.tar.gz` | 后端 Compose 和环境配置 | 启动 backend、celery |
| `260723yb-frontend-static-22.tar.gz` | 完整 `frontend` 目录 | 部署 Nginx、静态文件及保留前端工程 |

`SHA256SUMS` 用于校验以上三个包在复制、上传后是否损坏。

## 二、打包目录

所有命令默认从项目根目录开始：

```bash
cd /home/ubuntu/yibao/260723yb
```

## 三、构建前端

进入前端工程：

```bash
cd /home/ubuntu/yibao/260723yb/frontend/vue-vben-admin
```

先执行类型检查：

```bash
pnpm -F @vben/web-ele run typecheck
```

再执行生产构建：

```bash
pnpm -F @vben/web-ele run build
```

构建产物首先生成到：

```text
frontend/vue-vben-admin/apps/web-ele/dist/
```

将它同步到当前 Nginx 实际挂载的目录：

```bash
cp -a apps/web-ele/dist/. /home/ubuntu/yibao/260723yb/frontend/dist/
```

返回项目根目录：

```bash
cd /home/ubuntu/yibao/260723yb
```

## 四、准备新版本目录

以从 v21 升级为 v22 为例：

```bash
mkdir -p /home/ubuntu/yibao/260723yb/部署包/260723-22
```

下一次发布时，应顺序递增版本。例如从 v22 升级为 v23：

```text
基础版本：260723-22
目标版本：260723-23
```

## 五、生成后端镜像包

### 5.1 当前生成方式

当前没有重新执行完整的 Docker build，而是以上一版 OCI 镜像 TAR 为基础，追加最新 `/app` 文件层并修改镜像标签。

v22 使用：

```text
基础镜像包：部署包/260723-21/yibao_backend_260723-21.tar
最新应用目录：backend/app
部署设置文件：backend/deploy_settings.py
目标镜像标签：yibao_backend:260723-22
输出镜像包：部署包/260723-22/yibao_backend_260723-22.tar
```

### 5.2 执行打包脚本

```bash
./build/build_v22.sh
```

当前脚本位置：

```text
/home/ubuntu/yibao/260723yb/build/build_v22.sh
```

脚本执行内容：

1. 解开上一版 OCI 镜像 TAR。
2. 将 `backend/app` 同步到临时镜像层的 `/app`。
3. 排除 `__pycache__`、`*.pyc` 和 `.git`。
4. 将 `backend/deploy_settings.py` 写入镜像中的 `/app/deploy_settings.py`。
5. 计算新应用层 SHA256 和大小。
6. 更新 OCI config、manifest、index 和 repositories。
7. 将镜像标签更新为 `yibao_backend:260723-22`。
8. 生成可由 `docker load` 读取的镜像 TAR。

### 5.3 新版本脚本注意事项

制作 v23 时，可以复制 v22 脚本：

```bash
cp build/build_v22.sh build/build_v23.sh
chmod 0755 build/build_v23.sh
```

然后必须逐项确认：

```text
BASE：指向 v22 镜像 TAR
OUT：指向 v23 镜像 TAR
镜像标签：yibao_backend:260723-23
OCI ref.name：260723-23
repositories 标签：260723-23
history 中的版本说明：260723-23
```

特别注意：不能让 `BASE` 和 `OUT` 指向同一个文件。

## 六、更新后端 Compose 镜像标签

在生成后端部署包前，确认：

```text
/home/ubuntu/yibao/260723yb/backend/docker-compose.yml
```

backend 和 celery 两处镜像标签均为目标版本：

```yaml
image: yibao_backend:260723-22
```

可以检查：

```bash
rg -n "image: yibao_backend" backend/docker-compose.yml
```

## 七、生成后端部署包

执行：

```bash
tar -C /home/ubuntu/yibao/260723yb \
  -czf 部署包/260723-22/260723yb-backend-deploy-22.tar.gz \
  backend/docker-compose.yml \
  backend/.env \
  backend/.env.example
```

包内文件应为：

```text
backend/docker-compose.yml
backend/.env
backend/.env.example
```

检查命令：

```bash
tar -tzf 部署包/260723-22/260723yb-backend-deploy-22.tar.gz
```

注意：该包包含环境连接配置，必须限制读取权限和分发范围，不应上传到公开仓库。

## 八、生成前端包

当前打包方式包含整个 `frontend` 目录：

```bash
tar -C /home/ubuntu/yibao/260723yb \
  -czf 部署包/260723-22/260723yb-frontend-static-22.tar.gz \
  frontend
```

当前前端包包含：

- `frontend/dist`：Nginx 实际加载的静态文件；
- `frontend/nginx`：Nginx 配置；
- `frontend/docker-compose.yml`；
- `frontend/vue-vben-admin`：前端源码、构建产物和当前依赖目录。

因此该包不是只包含 `dist` 的精简包，体积会明显大于纯静态包。

## 九、生成 SHA256 校验文件

进入新版本部署目录：

```bash
cd /home/ubuntu/yibao/260723yb/部署包/260723-22
```

生成三个包的 SHA256：

```bash
sha256sum \
  yibao_backend_260723-22.tar \
  260723yb-backend-deploy-22.tar.gz \
  260723yb-frontend-static-22.tar.gz \
  > SHA256SUMS
```

统一文件权限：

```bash
chmod 0644 \
  yibao_backend_260723-22.tar \
  260723yb-backend-deploy-22.tar.gz \
  260723yb-frontend-static-22.tar.gz \
  SHA256SUMS
```

重新读取并校验：

```bash
sha256sum -c SHA256SUMS
```

三个包都应显示：

```text
OK
```

## 十、包内版本抽检

### 10.1 检查后端镜像标签

```bash
tar -xOf yibao_backend_260723-22.tar repositories
```

应看到：

```text
yibao_backend:260723-22
```

### 10.2 检查后端 Compose 标签

```bash
tar -xOzf 260723yb-backend-deploy-22.tar.gz \
  backend/docker-compose.yml | rg "image: yibao_backend"
```

backend 和 celery 两处都应是：

```text
image: yibao_backend:260723-22
```

### 10.3 检查前端包

先确认新构建的 `index.html` 已打入包内：

```bash
tar -tzf 260723yb-frontend-static-22.tar.gz | rg "frontend/dist/index.html"
```

对于本次 SQL 模块修改，还应确认最新前端产物包含“规则管理”“任务执行”，并且结果弹窗中不再包含“渲染后SQL”。

## 十一、目标服务器部署概要

加载后端镜像：

```bash
docker load -i yibao_backend_260723-22.tar
```

校验镜像：

```bash
docker image inspect yibao_backend:260723-22
```

解压后端部署包并启动：

```bash
tar -xzf 260723yb-backend-deploy-22.tar.gz
cd backend
docker compose up -d
```

解压前端包并启动：

```bash
tar -xzf 260723yb-frontend-static-22.tar.gz
cd frontend
docker compose up -d
```

前端 Compose 使用 Nginx，并将：

```text
./dist
```

只读挂载到：

```text
/usr/share/nginx/html
```

## 十二、发布前检查清单

- [ ] 前端类型检查通过。
- [ ] 前端生产构建成功。
- [ ] `apps/web-ele/dist` 已同步到 `frontend/dist`。
- [ ] 新版本部署目录已创建。
- [ ] 后端打包脚本的 `BASE` 指向上一版。
- [ ] 后端打包脚本的 `OUT` 指向新版本。
- [ ] OCI 镜像标签已更新为新版本。
- [ ] backend 和 celery 的 Compose 标签均已更新。
- [ ] 后端镜像包生成成功。
- [ ] 后端部署包生成成功。
- [ ] 前端包生成成功。
- [ ] `SHA256SUMS` 已生成。
- [ ] `sha256sum -c SHA256SUMS` 三项均为 `OK`。
- [ ] 三包内部版本和关键前端内容已抽检。
